ingress:
  enabled: true
  hosts:
    - host: gitea.anderssonfischer.local
      paths:
        - path: /
          pathType: Prefix

valkey-cluster:
  enabled: false
valkey:
  enabled: false
postgresql:
  enabled: false
postgresql-ha:
  enabled: false

gitea:
  config:
    database:
      DB_TYPE: sqlite3
    session:
      PROVIDER: memory
    cache:
      ADAPTER: memory
    queue:
      TYPE: level
    service:
      DISABLE_REGISTRATION: true
    server:
      SSH_DOMAIN: gitea.anderssonfischer.local
      SSH_PORT: 2222

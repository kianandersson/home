apiVersion: v1
kind: Secret
metadata:
  name: velero-credentials
  namespace: "{{ .Release.Namespace }}"
type: Opaque
stringData:
  cloud: "{{ .Values.credentials.gcp  | replace "\n" "" | fromJson | toJson }}"

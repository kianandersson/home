name: Gitea Actions Demo
run-name: ${{ gitea.actor }} is testing out Gitea Actions 🚀
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container: ghcr.io/catthehacker/ubuntu:act-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build apps/api -t gitea.anderssonfischer.com/kianandersson/api:${{ gitea.sha }}
      - run: docker push gitea.anderssonfischer.com/kianandersson/api:${{ gitea.sha }}
